# Semantic layer — DRAFT, generated by embedding_building/build_semantic_layer.py
#
# join_graph is an ALLOW-LIST. Once any edge exists here, validate_sql rejects
# every table pair that is NOT listed. Only sibling _PART_A/_PART_B pairs are
# proposed automatically; add other legitimate joins by hand, and delete any
# proposed edge that is not actually meaningful.
#
# Regenerating overwrites this file — copy your hand-authored edits first.

join_graph:
  - tables: [cims_raq_q_sec1_part_a_dom, cims_raq_q_sec1_part_b_dom]
    condition: "CIMS_RAQ_Q_SEC1_PART_A_DOM.CODE = CIMS_RAQ_Q_SEC1_PART_B_DOM.CODE AND CIMS_RAQ_Q_SEC1_PART_A_DOM.RDATE = CIMS_RAQ_Q_SEC1_PART_B_DOM.RDATE"
    note: "sibling parts of the same section; same entity and period"
  - tables: [cims_raq_q_sec2_part_a, cims_raq_q_sec2_part_b]
    condition: "CIMS_RAQ_Q_SEC2_PART_A.CODE = CIMS_RAQ_Q_SEC2_PART_B.CODE AND CIMS_RAQ_Q_SEC2_PART_A.RDATE = CIMS_RAQ_Q_SEC2_PART_B.RDATE"
    note: "sibling parts of the same section; same entity and period"
  - tables: [cims_raq_q_sec4_part_a, cims_raq_q_sec4_part_b]
    condition: "CIMS_RAQ_Q_SEC4_PART_A.CODE = CIMS_RAQ_Q_SEC4_PART_B.CODE AND CIMS_RAQ_Q_SEC4_PART_A.RDATE = CIMS_RAQ_Q_SEC4_PART_B.RDATE"
    note: "sibling parts of the same section; same entity and period"

vertical:
  cims_raq_q_sec10:
    label_column: description
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 21 distinct sampled values
  cims_raq_q_sec1_exc_govt_paper:
    label_column: description
    total_row: "Total"
    needs_trim: false
    # 4 distinct sampled values
  cims_raq_q_sec1_last_date_port:
    label_column: description
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 1 distinct sampled values
  cims_raq_q_sec1_part_a_dom:
    label_column: period_delinquency
    total_row: "C. Total ( A + B)"
    needs_trim: false
    # 13 distinct sampled values
  cims_raq_q_sec1_part_b_dom:
    label_column: period_delinquency
    total_row: "C. TOTAL (A+B)"
    needs_trim: false
    # 11 distinct sampled values
  cims_raq_q_sec1_part_c_o:
    label_column: period_delinquency
    total_row: "C. Total ( A + B)"
    needs_trim: false
    # 13 distinct sampled values
  cims_raq_q_sec1_part_d_o:
    label_column: period_delinquency
    total_row: "C. TOTAL (A+B)"
    needs_trim: false
    # 11 distinct sampled values
  cims_raq_q_sec2_last_date_port:
    label_column: description
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 2 distinct sampled values
  cims_raq_q_sec2_part_a:
    label_column: risk_category
    total_row: "Total (1 to 4)"
    needs_trim: false
    # 8 distinct sampled values
  cims_raq_q_sec2_part_a_npa:
    label_column: risk_category
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 2 distinct sampled values
  cims_raq_q_sec2_part_b:
    label_column: movement_provision_npa
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 6 distinct sampled values
  cims_raq_q_sec3_part_b:
    label_column: movement_restructure_std_la
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 14 distinct sampled values
  cims_raq_q_sec4_part_a:
    label_column: movement_from
    total_row: "Total Upgrade/Downgrade"
    needs_trim: false
    # 15 distinct sampled values
  cims_raq_q_sec4_part_b:
    label_column: movement_from
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 20 distinct sampled values
  cims_raq_q_sec8_ind_brkup:
    label_column: industry_name
    total_row: "All Industries (A to S)"
    needs_trim: false
    # 44 distinct sampled values
  cims_raq_q_sec8_infra_brkup:
    label_column: industry_name
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 50 distinct sampled values
  cims_raq_q_sec8_sec_credit:
    label_column: item
    total_row: "I. Gross Advances (II + III)"
    needs_trim: false
    # 50 distinct sampled values
  cims_raq_q_sec9_sensec_parta:
    label_column: description
    total_row: "Total Advances to Capital Market (1 to 6)"
    needs_trim: false
    # 24 distinct sampled values
  cims_raq_q_sec9_sensec_partb:
    label_column: description
    total_row: "A Total Credit"
    needs_trim: false
    # 37 distinct sampled values
  cims_raq_q_sec_12_misc:
    label_column: item
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 10 distinct sampled values
  cims_raq_q_sec_12_misc_t2:
    label_column: category
    total_row: null   # no total row detected — check by hand
    needs_trim: false
    # 7 distinct sampled values
  cims_raq_q_sec_12_misc_t3:
    label_column: description
    total_row: "Total Retail Advances"
    needs_trim: false
    # 7 distinct sampled values
  cims_raq_q_sec_12_misc_t4:
    label_column: description
    total_row: "Total Advances"
    needs_trim: false
    # 3 distinct sampled values

dom_ove:
  cims_raq_q_sec9_sensec_parta:
    - [exposure_dom, exposure_ove]
    - [gross_amt_os_dom, gross_amt_os_ove]
    - [of_which_impaired_dom, of_which_impaired_ove]
  cims_raq_q_sec9_sensec_partb:
    - [amt_os_funded_dom, amt_os_funded_ove]
    - [impaired_dom, impaired_ove]
    - [restru_std_dom, restru_std_ove]
    - [slippage_dom, slippage_ove]
    - [tot_expo_dom, tot_expo_ove]

multipart:
  - [cims_raq_q_sec1_part_a_dom, cims_raq_q_sec1_part_b_dom]
  - [cims_raq_q_sec2_part_a, cims_raq_q_sec2_part_b]
  - [cims_raq_q_sec4_part_a, cims_raq_q_sec4_part_b]

# Named business definitions. Hand-authored: nothing in the DDL states these.
# Any metric named here that appears in a question is injected into the prompt.
metrics: {}

# Query-side vocabulary. Promote the regexes in src/retriever.py:_QUERY_EXPANSIONS
# here as they are reviewed, so the vocabulary is data rather than code.
synonyms: {}
