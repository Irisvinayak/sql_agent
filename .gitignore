# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Vector indexes (binary, rebuilt via python embedding_building/main.py)
# embedding_building/output/*.faiss
# embedding_building/output/*.pkl

# Frontend
frontend/node_modules/
frontend/dist/

# Environment / secrets
.env
*.env

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Scratch: measurement baselines, index rollbacks, ad-hoc reports. Useful
# locally (scripts/eval_retrieval.py --compare reads the retr_*.json baselines
# from here) but not shared state. Nothing in the pipeline reads this directory —
# scratch/sql_generation_context_report.md predates the rule and stays tracked.
scratch/
